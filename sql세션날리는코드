sqlplus "/as sysdba"로 들어가서


SELECT A.SID, A.SERIAL#
  FROM V$SESSION A, V$LOCK B, DBA_OBJECTS C
  WHERE A.SID = B.SID
  AND B.ID1 = C.OBJECT_ID
  AND B.TYPE = 'TM'
  AND C.OBJECT_NAME = 'SONGS';


ALTER SYSTEM KILL SESSION '17, 399';


세션이랑 시리얼 찾아서 날리면됨

